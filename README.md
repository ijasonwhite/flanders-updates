# Flanders — update feed

This repository hosts the [Sparkle](https://sparkle-project.org) update feed
(`appcast.xml`) and notarized release archives for the Flanders MySQL client.

**No source code lives here.** Flanders itself is closed-source; this repo
exists only so installed copies can find and verify updates.
